

let f = {
loc n = {
      loc c = 0;
      while c<x{
        print " ";
        set c = c+1;
        
      };
      
    };
loc x = 0;
loc max = 100
    while True{
      set x = (x+1)%max;
      n(sin(x/30)*max);
      print "Hello!endl"
      sleep(120);
    };
    
  };

f()
