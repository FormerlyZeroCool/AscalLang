

let f = {
loc n = {
      loc c = 0;
      while c<x{
        print " ";
        set c = c+1;
        
      };
      
    };
loc x = 0;

    while True{
      set x = (x+1)%max;
      n(max+sin(x/(max/(2*pi)))*max);
      print "Hello!endl";
      sleep(220);
    };
    
  };

f(60)
