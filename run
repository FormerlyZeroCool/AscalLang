FILE=./ascal
if [ -f "$FILE" ]; then
echo "skipping build ./ascal exists"
else 
./build.sh
fi
echo "Starting Ascal interpreter"
echo "Running, awaiting input, try typing fibr(3)"
echo "That will calculate the third fibonacci number in the sequence"
./ascal
